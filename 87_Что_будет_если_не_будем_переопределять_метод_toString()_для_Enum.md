Если не переопределить метод toString() для Enum, то при вызове метода toString() будет возвращаться имя константы в виде строки, т.е. имя элемента перечисления (enum), определенного в исходном коде.

